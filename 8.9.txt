
value값에따라 페이지가 달라지려면 urls에 있는 값에id 값을 달아주고 
해당 html의 for문안에 해당 값에도 id값을 넣어줘야함!!
ex)
1.  path('econtent/<int:id>', views.econtent, name='econtent'),
2. <td><a href="../econtent/{{ev.id}}">{{ev.content}}</a></td>


event_list = [event,event1,event2]
for x in event_list: x.save()

Event.objects.all().values()   (for 문으로 save 1번으로 끝내기)